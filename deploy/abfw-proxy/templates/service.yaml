apiVersion: v1
kind: Service
metadata:
  name: {{ include "abfw-proxy.fullname" . }}
  labels:
    {{- include "abfw-proxy.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "abfw-proxy.selectorLabels" . | nindent 4 }}
